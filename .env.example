# ============================================================================
# Astraeus — environment configuration
# Copy this file to .env and fill in your keys. NEVER commit .env (it's gitignored).
# .env and this file share the SAME variables; this file just has blank secrets.
# ============================================================================

# ---- Required: model + data ----
# OpenRouter — LLM reasoning & forecasts (https://openrouter.ai/keys)
OPENROUTER_API_KEY=
# CoinMarketCap AI Agent Hub — market data + Skill Hub (https://coinmarketcap.com/api/agent)
COINMARKETCAP_API_KEY=

# ---- Security ----
# Salt used by ElizaOS to encrypt secrets it persists in its database (character
# settings.secrets and any runtime-saved secrets). If unset it falls back to the
# hardcoded default "secretsalt" — i.e. a publicly known key — and core warns at boot.
# Set a strong, unique value BEFORE any secrets are stored (changing it later makes
# already-encrypted secrets undecryptable). Generate one with:
#   bun -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET_SALT=

# Model selection is NOT configured here — it lives entirely in src/character.ts
# (settings.model / embeddingModel / the OPENROUTER_*_MODEL keys / settings.models).
# Edit the model IDs there; no env vars are involved.

# ---- Autonomous trading loop ----
# What to do on boot — space-separated flags (case-insensitive):
#   trade  → AUTO-START the autonomous loop + Track-1 heartbeat on boot, and resume the
#            cadence after a restart (also accepts auto/on/true). REQUIRED for the live
#            week; leave blank to keep the loop off and drive it from chat ("start auto").
#   cmc    → fold CMC options-positioning analysis (Skill Hub "source 7") into every loop
#            forecast — forces it on even if CMC_OPTIONS_ENRICH=false, at the cost of the
#            slow MCP call's latency. (With CMC_OPTIONS_ENRICH=true it's already on.)
# Example: AUTONOMOUS_MODE=trade cmc
AUTONOMOUS_MODE=
# Symbols to trade (comma-separated; must be on the eligible BEP-20 allowlist).
TRADING_SYMBOLS=ETH, USDT
# Timeframe for each autonomous trade: hourly | 4h | daily | weekly. Each position
# auto-closes after its timeframe (hourly→1h, 4h→4h, daily→24h, weekly→7d).
AUTONOMOUS_TIMEFRAME=4h
# Milliseconds between autonomous ticks on the happy path (14400000 = 4 hours).
AUTONOMOUS_INTERVAL_MS=14400000
# On an UNSUCCESSFUL auto-trade (ETH not forecast UP with conviction >= RISK_MIN_CONVICTION),
# retry on a shorter timeframe after this delay (3600000 = 1 hour): daily→4h→1h→1h…
# until a trade opens, then reset to the 12h base cadence. Unsuccessful attempts don't count.
AUTONOMOUS_RETRY_MS=3600000
# ---- Custom WITHDRAW position timeline — MAIN loop (ETH) ----
# Override how long each MAIN-loop position is held before it auto-closes ("withdraws"),
# decoupled from the forecast timeframe's default (hourly→1h, 4h→4h, daily→24h, weekly→7d).
# Two parts: an ON/OFF switch and the duration.
#   AUTONOMOUS_WITHDRAW_TIMELINE_ENABLED  → true turns the override ON; false (default) keeps
#                                           the timeframe default. The value below does nothing
#                                           until this is true.
#   AUTONOMOUS_WITHDRAW_TIMELINE          → the hold duration. Can be SHORTER or LONGER than the
#                                           timeframe. Accepts units s/m/h/d/w ("3d", "8h",
#                                           "30m", "1.5h") or a bare number of milliseconds.
#                                           Capped at ~24.8 days (the auto-close timer's limit).
# Example: timeframe is daily (24h) but you want to hold 3 days → ENABLED=true, value=3d. Or
# hold only 8h → value=8h. Blank/invalid value with the switch on falls back to the timeframe.
AUTONOMOUS_WITHDRAW_TIMELINE_ENABLED=false
AUTONOMOUS_WITHDRAW_TIMELINE=2d
# Paper-trading starting cash in USD (PAPER MODE ONLY — fake money, not real funds).
PAPER_START_CASH_USD=57

# ---- Track 1 trade heartbeat (competition daily-trade guarantee) ----
# A STANDALONE guarantee that runs INDEPENDENTLY of the trading loop: every
# TRACK1_QUALIFY_INTERVAL_MS (18h) the agent buys the TRACK1_QUALIFY_TRADE_USD size of ETH
# ($1.5) and immediately sells it back to USDT — a round-trip that registers a competition
# trade with ~zero market exposure (cost = fees + slippage). On success the next one is
# one interval later; if a round-trip FAILS it retries every TRACK1_QUALIFY_RETRY_MS until
# it lands. It never collides with the trading loop — if a loop cycle is running it waits
# for that to finish (success or no-position) first. OFF by default; turn ON for live week.
TRACK1_QUALIFY_ENABLED=true
# Cadence of the guaranteed qualifying round-trip (64800000 = 18 hours). Change this to
# tune how often the heartbeat trade fires.
TRACK1_QUALIFY_INTERVAL_MS=64800000
# USD size of the qualifying round-trip — INDEPENDENT of the main loop's RISK_MAX_TRADE_USD.
# Kept small ($1.5) so the daily-trade guarantee costs only fees + slippage, not real size.
TRACK1_QUALIFY_TRADE_USD=1.05
# If a qualifying round-trip FAILS (no price / swap error / insufficient cash), retry it
# this often (300000 = 5 minutes) until it lands.
TRACK1_QUALIFY_RETRY_MS=60000
# Self-heal for a round-trip whose SELL-BACK leg failed (e.g. a live revert) and left the
# bought ETH stranded in the wallet. Before each round-trip — and once on startup — the agent
# FLATTENS that stray ETH back to USDT, selling ONLY the amount beyond any tracked main-loop
# position and with the close haircut so it can never over-request. Set false to disable the
# self-heal (NOT recommended for the live week).
TRACK1_FLATTEN_STRAY=true
# Don't bother flattening stray ETH worth less than this (USD) — dust left by the sell haircut
# isn't worth a swap's gas/fees, and the round-trip counts as clean below it (0.5 = $0.50).
TRACK1_ETH_DUST_USD=0.04
# Block manual "forecast and trade BTC/BNB" in LIVE mode (true = block, default). BTC and
# BNB aren't on the competition's eligible token list, so a real buy wastes USDT on
# something that won't count (and may not route on TWAK). ETH is always allowed, and the
# autonomous loop only trades ETH. Set false to allow live BTC/BNB; paper mode is never blocked.
TRACK1_BLOCK_BTC_BNB=true

# Restrict the TRENDING action to ONLY your watchlist symbols — filtered in code (not the
# LLM), so off-list tokens can never appear. Off (default) = TRENDING returns CMC's global
# trending feed as before. On = filter to your watchlist, so even a plain "what's trending?"
# is filtered (no need to paste the list into every command). The watchlist is, in order:
# an inline "…in this list: ETH, MYX" from the message, else TRACK1_TRENDING_WATCHLIST
# below, else the baked-in default 146-token list.
TRACK1_TRENDING_WATCHLIST_FILTER=true
# Optional: override the default watchlist with your own comma-separated symbols. Leave
# unset to use the baked-in default 146-token list. Only used when the filter is on and the
# message carries no inline list.
# TRACK1_TRENDING_WATCHLIST=ETH,USDT,USDC,XRP,BTC
# How many trending tokens to pull from CMC before filtering to your list (the search
# window). Bigger = more of your list can match, but rank-N is only loosely "trending".
# Only used when TRACK1_TRENDING_WATCHLIST_FILTER=true. Default 30, clamped to 1–100.
TRACK1_TRENDING_SEARCH_CAP=30
# How many trending tokens TRENDING reports when the message names no explicit count
# (normal mode). An explicit number in the message ("top 5 trending") still wins and is
# clamped to 1–20. Default 10, clamped to 1–100.
TRENDING_DEFAULT_LIMIT=10

# ---- Track 1: high-risk altcoin trades (opt-in) ----
# When ON, an ETH forecast cycle that comes back BULLISH (up) or SIDEWAYS is DIVERTED: instead of the
# normal ETH decision (buy on a confident UP, skip on sideways) the agent hunts altcoins for that cycle —
# searches the trending feed (ALWAYS filtered to your eligible BSC list — TRACK1_TRENDING_WATCHLIST, else
# the baked-in default; this on-list guardrail is unconditional and independent of the TRENDING display toggle),
# researches the top candidates one at a time, and BUYS the FIRST whose research read is bullish with
# confidence >= TRACK1_ALTCOIN_MIN_CONFIDENCE. If none of the top TRACK1_ALTCOIN_SCAN_DEPTH qualify it
# opens nothing and tries again after TRACK1_ALTCOIN_RETRY_MS. Buys can ONLY land on the eligible BSC
# watchlist. OFF by default (the loop just trades ETH as normal).
TRACK1_ALTCOIN_TRADES_ENABLED=true
# Which chart timeframe each per-candidate research read uses: 1h | 4h | daily | weekly. Default 1h.
TRACK1_ALTCOIN_RESEARCH_TIMEFRAME=1h
# How many of the top trending BSC tokens to research before giving up this cycle. Default 5 (1–50).
TRACK1_ALTCOIN_SCAN_DEPTH=5
# Minimum bullish research confidence (%) required to BUY a candidate. Default 60 (0–100).
TRACK1_ALTCOIN_MIN_CONFIDENCE=60
# "Try again later" delay when none of the top N qualify (3600000 = 1 hour). Floored at 60s.
TRACK1_ALTCOIN_RETRY_MS=3600000
# Hand TWAK each altcoin's BSC CONTRACT ADDRESS (from the baked src/config/bsc-contracts.ts map,
# resolved once from CoinMarketCap — the agent never calls the API per trade) instead of its ticker,
# so a live swap can't route the wrong token: tickers like M/B/U/NFT/TON collide across chains.
# Default true; set false to route by symbol (only safe for unambiguous tickers like ETH/USDT).
TRACK1_ALTCOIN_USE_CONTRACT=true
# ---- Custom WITHDRAW position timeline — ALT loop (high-risk altcoins) ----
# Same as the MAIN-loop override above, but for high-risk altcoin positions, and fully
# INDEPENDENT of it (set the two separately). An ON/OFF switch plus a duration:
#   TRACK1_ALTCOIN_WITHDRAW_TIMELINE_ENABLED → true turns it ON; false (default) keeps the
#                                              timeframe default. The value below is inert until true.
#   TRACK1_ALTCOIN_WITHDRAW_TIMELINE         → hold duration, SHORTER or LONGER than the timeframe.
#                                              Units s/m/h/d/w ("8h", "2d", "45m") or bare ms.
# Example: hold every altcoin position 8h regardless of the forecast timeframe → ENABLED=true, value=8h.
TRACK1_ALTCOIN_WITHDRAW_TIMELINE_ENABLED=true
TRACK1_ALTCOIN_WITHDRAW_TIMELINE=8h

# ---- Risk guardrails (optional; defaults shown) ----
RISK_MAX_DRAWDOWN_PCT=29
RISK_MAX_TRADE_USD=57
RISK_MAX_TRADES_PER_DAY=10
RISK_MAX_DAILY_VOLUME_USD=57
RISK_MIN_TRADES_PER_DAY=3
RISK_MAX_SLIPPAGE_BPS=100
RISK_MIN_CONVICTION=60
RISK_MIN_CASH_RESERVE_PCT=0
# When CLOSING a position, sell this much LESS than the held amount (basis points,
# 50 = 0.5%) so a price tick / fee / rounding between the balance read and the swap can
# never request more than the wallet holds (a live TWAK swap rejects an over-balance
# amount, which would leave the position stuck open). Leaves negligible dust.
CLOSE_SELL_HAIRCUT_BPS=50
# When CLOSING a position, RETRY the live price quote this many times before giving up on it
# and sizing the sell off the wallet's CURRENT on-chain valuation (the entry/buy price is only
# the last resort). CMC's quote service is intermittently flaky, so we try hard for a fresh
# price at the sell-off moment. Default 5 (floored at 1).
CLOSE_PRICE_RETRIES=5
# Delay between those price-quote retries, in ms. Default 1500.
CLOSE_PRICE_RETRY_MS=1500

# ---- Take-profit (the OPPOSITE of the drawdown cap) ----
# Close a position EARLY once its unrealized gain reaches RISK_TAKE_PROFIT_PCT %, locking
# in the profit instead of waiting out the timeframe. The monitor checks open positions
# every RISK_TAKE_PROFIT_CHECK_MS while the loop runs. Toggle with RISK_TAKE_PROFIT_ENABLED
# (code default is OFF; set true to use it).
RISK_TAKE_PROFIT_ENABLED=true
RISK_TAKE_PROFIT_PCT=4.5
RISK_TAKE_PROFIT_CHECK_MS=300000

# ---- Live trading on BNB Chain (Track 1 — NOT active until Trust Wallet Agent Kit is wired) ----
# Master switch for real on-chain trading. Keep false until tested in paper mode.
ENABLE_LIVE_TRADING=true
# BNB Smart Chain RPC + chain id (56 = BSC mainnet).
BSC_RPC_URL=https://bsc-dataseed.binance.org
CHAIN_ID=56
# Trust Wallet Agent Kit credentials (from portal.trustwallet.com).
TWAK_ACCESS_ID=
TWAK_HMAC_SECRET=
# Wallet password for HEADLESS / Docker signing ONLY (server, container, CI/CD).
# TWAK reads it here when there's no OS keychain. Resolution order: --password flag >
# TWAK_WALLET_PASSWORD > OS keychain. On your laptop, leave this BLANK and instead run
# `twak wallet keychain save`. NEVER commit a real value — use a Docker secret in prod.
TWAK_WALLET_PASSWORD=
# Retry a swap up to this many times on TRANSIENT errors only (TWAK price-service blips /
# NETWORK_ERROR / "unable to fetch price" / "fail to fetch") — real reverts, auth, and
# insufficient-funds are NEVER retried. TWAK_SWAP_RETRY_MS is the spacing between attempts (ms).
TWAK_SWAP_RETRIES=10
TWAK_SWAP_RETRY_MS=2000
# Hard timeout for a single live swap (ms). A swap is TWO sequential on-chain txs (approval +
# swap) via an aggregator — ~200s observed on BSC — so it needs more headroom than a read.
# Too low and twak gets killed AFTER broadcasting (phantom fill, reconciled next cycle).
TWAK_SWAP_TIMEOUT_MS=240000
# Your agent's BSC wallet address (used for on-chain competition registration).
AGENT_WALLET_ADDRESS=
# BNB Hack Track 1 competition contract.
COMPETITION_CONTRACT_ADDRESS=0x212c61b9b72c95d95bf29cf032f5e5635629aed5
# Max auto-approved x402 payment per request, in atomic units (10000 = 0.011 USDC at 6dp).
X402_MAX_PAYMENT=11000
# Chain to settle x402 payments on (default "base"). CMC's x402 endpoints advertise
# their PREFERRED route as a stablecoin on BSC priced in 18 decimals (0.01 = 1e16 atomic),
# which exceeds the 6-dp X402_MAX_PAYMENT cap (PAYMENT_AMOUNT_EXCEEDED). Pinning to Base
# uses the 10000-atomic USDC (6dp) route — within the cap and the chain the wallet holds
# USDC on. Set to "bsc" or a CAIP id (e.g. eip155:8453) to override, or "" to let TWAK pick.
X402_PREFER_NETWORK=base
# Optional: pay an x402-gated endpoint each trade cycle to enrich the forecast.
# OFF by default. Best-effort: bounded by X402_ENRICH_TIMEOUT_MS and falls back to
# the free data path on any failure — it can never block or delay a trade beyond the timeout.
X402_ENRICH=true
# CMC AI Agent Hub x402 endpoints — COMMA-SEPARATED, each $0.01 USDC on Base per call,
# fetched in parallel and merged into the forecast. Flip X402_ENRICH=true once the
# TWAK wallet holds a little USDC on Base. Add/remove from these CMC x402 paths:
#   /x402/v3/cryptocurrency/quotes/latest?symbol=ETH   (ETH quote)
#   /x402/v3/cryptocurrency/listings/latest            (top-market regime context)
#   /x402/v4/dex/pairs/quotes/latest                   (DEX pair quotes)
#   /x402/v1/dex/search?q=eth                          (DEX search)
X402_DATA_URL=https://pro-api.coinmarketcap.com/x402/v3/cryptocurrency/quotes/latest?symbol=ETH,https://pro-api.coinmarketcap.com/x402/v1/dex/search?q=eth
X402_ENRICH_TIMEOUT_MS=25000

# ---- CMC AI Agent Hub forecast enrichment (supplementary ~40%; options data stays ~60%) ----
# CMC technicals (RSI/MACD/EMA) + market regime (total cap, BTC dominance) + Fear & Greed
# + current CMC price, fed into the ETH forecast. ON by default; set false to disable.
# Bounded + best-effort: a slow/failed CMC call is skipped and the forecast falls back to options.
CMC_ENRICH=true
CMC_ENRICH_TIMEOUT_MS=60000
# ---- MCP server CONNECT timeout (handshake at startup, NOT per-call) ----
# @elizaos/plugin-mcp hardcodes a 60s cap on client.connect() for each MCP server
# (cmc-skill-hub / trust-wallet) and ignores the per-server `timeout` in character.ts.
# A slow/cold handshake (CloudFront cold edge, Cloudflare WARP) gets killed at 60s →
# "Timeout connecting to cmc-skill-hub". scripts/patch-mcp-timeout.mjs (postinstall)
# rewrites that literal to honor this var. Raise it to give the connect room.
MCP_CONNECT_TIMEOUT_MS=180000
# CMC options-positioning analysis (Skill Hub MCP) as forecast "source 7" in the loop.
# ON by default; bounded so the slow MCP call can NEVER stall the loop.
CMC_OPTIONS_ENRICH=true
CMC_OPTIONS_TIMEOUT_MS=150000
# Include source 7 (CMC options-positioning) in the MARKET overview's options leg.
# OFF by default — it adds ~CMC_OPTIONS_TIMEOUT_MS of latency (slow MCP). Set true to enable.
MARKET_OPTIONS_ENRICH=true
# ---- Forecast & market weights (tune the blend without code changes) ----
# All weights below take ANY scale (e.g. 50/30/20 or 0.5/0.3/0.2) and are normalized
# internally — only the RATIO between them matters. Leave unset to use the defaults shown.
#
# "forecast BTC/ETH/BNB" (per-asset OPTIONS_FORECAST) — how the LLM should weight the
# supplementary CoinMarketCap context: options & derivatives / technicals (RSI,MACD,EMA) /
# ETF-flow & other paid context. Defaults 50/30/20.
FORECAST_WEIGHT_OPTIONS=50
FORECAST_WEIGHT_TECHNICALS=30
FORECAST_WEIGHT_ETF=20
# Individual options-signal weights inside each per-asset forecast (relative importance of
# each indicator in the aggregated sentiment). Defaults shown.
SIGNAL_WEIGHT_IV=0.15
SIGNAL_WEIGHT_DVOL=0.12
SIGNAL_WEIGHT_PCR=0.15
SIGNAL_WEIGHT_MAXPAIN=0.1
SIGNAL_WEIGHT_FLOW=0.15
SIGNAL_WEIGHT_FUNDING=0.12
SIGNAL_WEIGHT_LIQUIDATION=0.1
SIGNAL_WEIGHT_RISK_REVERSAL=0.11
# "market status overview" (MARKET_FORECAST) — how much each major counts toward the
# weighted overall read. Defaults BTC 50 / ETH 30 / BNB 20.
MARKET_WEIGHT_BTC=50
MARKET_WEIGHT_ETH=30
MARKET_WEIGHT_BNB=20
# "market status overview" base blend — a SINGLE weighted mix of three legs on the same
# scale (any numbers; only their ratio matters). A leg drops out if its data is unavailable
# and the rest are renormalized. Defaults 56 / 24 / 20:
#   MARKET_CMC_WEIGHT     — CMC REST market data (Fear & Greed, total cap, BTC technicals)
#   MARKET_OPTIONS_WEIGHT — the per-asset BTC/ETH/BNB options forecasts
#   MARKET_SKILL_WEIGHT   — the CMC Skill Hub bundle (only when CMC_SKILLS_ENABLED=true)
MARKET_CMC_WEIGHT=55
MARKET_OPTIONS_WEIGHT=25
MARKET_SKILL_WEIGHT=20
# Per-asset forecast LLM call (OpenRouter) — retry on TRANSIENT failures only: network
# blips ("Unable to connect" / socket resets) and 408/429/5xx. Auth/400/other 4xx fail
# fast (no retry). FORECAST_LLM_RETRIES = total attempts; FORECAST_LLM_RETRY_MS = backoff
# (grows per attempt: 1×, 2×, 3×…). Defaults 3 / 1500ms.
FORECAST_LLM_RETRIES=3
FORECAST_LLM_RETRY_MS=1500
# ---- CMC skill bundles (qualitative Skill Hub analyses — SLOW, off by default) ----
# Master toggle for the per-feature CMC skill bundles added to MARKET overview / TRENDING /
# RESEARCH. OFF by default — each runs many slow execute_skill calls (~tens of sec each).
# Override the lists with MARKET_SKILLS / TRENDING_SKILLS / RESEARCH_SKILLS (comma-separated
# skill unique_names); leave blank to use the built-in defaults.
CMC_SKILLS_ENABLED=true
CMC_SKILLS_TIMEOUT_MS=150000
# Max concurrent execute_skill calls per bundle. The hub returns 502 Bad Gateway when a
# whole bundle (~100 calls) is fired at once, so calls are pooled. 6 is safe; raise for
# speed only if the hub tolerates it. Applies to both the live probe and auto-enrichment.
CMC_SKILLS_CONCURRENCY=20
# Show the raw "CMC SKILL ANALYSES" bundle dump in MARKET / RESEARCH / TRENDING replies when
# the LLM can't synthesize it into a one-line read. OFF by default — the raw dump is noisy
# (e.g. "exchange_market_structure_monitor:BTC: …venue_concentration"); the synthesized "CMC
# skill read" line is the intended signal, and the bundle still feeds the score either way.
# Set true to restore the dump. LIQUIDATION/PORTFOLIO are unaffected (the bundle IS their output).
CMC_SKILLS_SHOW_RAW=false
# (MARKET_SKILL_WEIGHT — how much this bundle moves the "market status overview" score —
# lives with the other market-overview weights in the "Forecast & market weights" block above.)
# Agent debug report (chat: "debug skill bundles"). The live probe runs EVERY skill in a
# bundle by default (spends CMC credits). Optional NUMERIC cap: N = first N skills/bundle,
# 0 = all (non-numeric is ignored). In chat, say "quick" for a fast 3/bundle spot-check.
DEBUG_SKILL_PROBE_LIMIT=0

# ---- BNB AI Agent SDK extras (optional, prize features) ----
# ERC-8004 on-chain agent identity command (AGENT_IDENTITY). Set false to disable it.
ERC8004_IDENTITY_ENABLED=true
# ERC-8183 sidecar bridge: when true, the agent publishes each forecast to
# data/latest-forecast.json so the optional Python sidecar can sell it as an on-chain
# job. OFF by default — turn on only when running the sidecar (see sidecar/README.md).
ERC8183_SIDECAR_ENABLED=true

# ---- Local database (optional) ----
# ElizaOS uses a local PGlite DB by default. Uncomment to override its location,
# or set POSTGRES_URL to use Postgres instead. NOTE: a custom PGLITE_DATA_DIR with a
# Windows backslash path can be mangled by dotenv and break DB writes (e.g. the
# central_messages insert failing, which silently drops the agent's reply) — use a
# forward-slash path, e.g. C:/Users/you/astraeus/.eliza/.elizadb
# PGLITE_DATA_DIR=
# POSTGRES_URL=

# ---- Logging (optional) ----
# LOG_LEVEL=info